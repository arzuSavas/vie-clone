import React from 'react'

const SubHeading=({title,subhead})=> (
 
    <div>
        <h6 className='title-style'>{title}</h6>
        <h3 className='subhead-style'>{subhead}</h3>
    </div>
  
);

export default SubHeading